/** 2017-12-18 22:24:13
 *作者:htl
 *功能:工具类
 */
let util = {
    http(option) {
        let data = option.data || {},
            method = option.method || 'GET',
            url = option.url,
            success = option.success || undefined,
            error = option.error || undefined;

    }
}
export default util;